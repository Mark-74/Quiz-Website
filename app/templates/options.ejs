<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #e8f5e9; 
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        h1 {
            color: #388e3c; 
            font-size: 36px;
            margin-bottom: 30px;
        }
        .button-container {
            display: flex;
            flex-direction: row; 
            justify-content: center; 
            gap: 20px; 
        }
        .button-container button {
            padding: 12px 25px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            border: 2px solid #388e3c;
            border-radius: 8px;
            background-color: #66bb6a; 
            color: white;
            transition: background-color 0.3s, transform 0.2s;
        }
        .button-container button:hover {
            background-color: #43a047; 
            transform: translateY(-3px);
        }
        .no-buttons {
            font-size: 18px;
            color: #388e3c;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        <div class="button-container">
            <% if (buttons && buttons.length > 0) { %>
                <% buttons.forEach(function(buttonText) { %>
                    <button onclick="window.location.href='/quiz/<%= buttonText %>';"> <%= buttonText %></button>
                <% }); %>
            <% } else { %>
                <p class="no-buttons">No buttons to display.</p>
            <% } %>
        </div>
    </div>
</body>
</html>
